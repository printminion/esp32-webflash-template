# ============================================================
# release.yml
# Triggered on version tags (v*).
# Builds all release PlatformIO environments, creates a
# GitHub Release and attaches firmware .bin files as assets.
# Also updates the web installer manifest and deploys to
# GitHub Pages.
# ============================================================

name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build firmware
    runs-on: ubuntu-latest

    strategy:
      matrix:
        environment:
          - seeed_xiao_esp32c3
          - seeed_xiao_esp32s3
          - seeed_xiao_esp32c6
          - generic_esp32

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2

      - name: Cache PlatformIO
        uses: actions/cache@v4.2.0
        with:
          path: ~/.platformio
          key: platformio-${{ runner.os }}-${{ hashFiles('platformio.ini') }}

      - name: Set up Python
        uses: actions/setup-python@v5.4.0
        with:
          python-version: '3.11'

      - name: Install PlatformIO
        run: pip install platformio

      - name: Pre-install uv (required by pioarduino platform for ESP32-C6)
        if: matrix.environment == 'seeed_xiao_esp32c6'
        run: pip install uv

      - name: Inject firmware version
        run: |
          VERSION="${{ github.ref_name }}"
          sed -i "s/-D FIRMWARE_VERSION='\"dev\"'/-D FIRMWARE_VERSION='\"${VERSION}\"'/" platformio.ini

      - name: Build ${{ matrix.environment }}
        run: pio run -e ${{ matrix.environment }}

      - name: Rename binary
        run: |
          mkdir -p firmware
          cp .pio/build/${{ matrix.environment }}/firmware.bin \
             firmware/firmware-${{ matrix.environment }}-${{ github.ref_name }}.bin

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4.6.0
        with:
          name: firmware-${{ matrix.environment }}
          path: firmware/*.bin
          retention-days: 7

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build

    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2
        with:
          ref: main
          fetch-depth: 1

      - name: Download all firmware artifacts
        uses: actions/download-artifact@v4.2.0
        with:
          path: firmware
          merge-multiple: true

      - name: List firmware files
        run: ls -lh firmware/

      - name: Copy firmware to docs for CORS-safe GitHub Pages hosting
        run: |
          VERSION="${{ github.ref_name }}"
          mkdir -p docs/firmware/${VERSION}
          cp firmware/*.bin docs/firmware/${VERSION}/

      - name: Generate manifest.json and version.json
        run: |
          VERSION="${{ github.ref_name }}"
          # manifest.json: served by the browser — must use GitHub Pages URLs (CORS-enabled)
          PAGES_URL="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/firmware/${VERSION}"
          # version.json: fetched by firmware via esp_https_ota — GitHub Releases URLs are fine
          RELEASES_URL="https://github.com/${{ github.repository }}/releases/download/${VERSION}"

          cat > docs/manifest.json << EOF
          {
            "name": "esp32-webflash-template",
            "version": "${VERSION}",
            "channel": "release",
            "builds": [
              {
                "chipFamily": "ESP32-C3",
                "board": "Seeed XIAO ESP32-C3",
                "parts": [
                  {
                    "path": "${PAGES_URL}/firmware-seeed_xiao_esp32c3-${VERSION}.bin",
                    "offset": 0
                  }
                ]
              },
              {
                "chipFamily": "ESP32-S3",
                "board": "Seeed XIAO ESP32-S3",
                "parts": [
                  {
                    "path": "${PAGES_URL}/firmware-seeed_xiao_esp32s3-${VERSION}.bin",
                    "offset": 0
                  }
                ]
              },
              {
                "chipFamily": "ESP32-C6",
                "board": "Seeed XIAO ESP32-C6",
                "parts": [
                  {
                    "path": "${PAGES_URL}/firmware-seeed_xiao_esp32c6-${VERSION}.bin",
                    "offset": 0
                  }
                ]
              },
              {
                "chipFamily": "ESP32",
                "board": "Generic ESP32",
                "parts": [
                  {
                    "path": "${PAGES_URL}/firmware-generic_esp32-${VERSION}.bin",
                    "offset": 0
                  }
                ]
              }
            ]
          }
          EOF

          cat > docs/version.json << EOF
          {
            "version": "${VERSION}",
            "boards": {
              "Seeed XIAO ESP32-C3": "${RELEASES_URL}/firmware-seeed_xiao_esp32c3-${VERSION}.bin",
              "Seeed XIAO ESP32-S3": "${RELEASES_URL}/firmware-seeed_xiao_esp32s3-${VERSION}.bin",
              "Seeed XIAO ESP32-C6": "${RELEASES_URL}/firmware-seeed_xiao_esp32c6-${VERSION}.bin",
              "Generic ESP32":       "${RELEASES_URL}/firmware-generic_esp32-${VERSION}.bin"
            }
          }
          EOF

      - name: Commit updated manifest
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/manifest.json docs/version.json
          git diff --staged --quiet || git commit -m "chore: update manifests for ${{ github.ref_name }}"
          git push origin HEAD:main

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          body: |
            ## esp32-webflash-template ${{ github.ref_name }}

            ### Flash via web installer
            👉 https://printminion.github.io/esp32-webflash-template/

            ### Supported boards
            | Board | Firmware file |
            |---|---|
            | Seeed XIAO ESP32-C3 | `firmware-seeed_xiao_esp32c3-${{ github.ref_name }}.bin` |
            | Seeed XIAO ESP32-S3 | `firmware-seeed_xiao_esp32s3-${{ github.ref_name }}.bin` |
            | Seeed XIAO ESP32-C6 | `firmware-seeed_xiao_esp32c6-${{ github.ref_name }}.bin` |
            | Generic ESP32 | `firmware-generic_esp32-${{ github.ref_name }}.bin` |
          files: firmware/*.bin
          draft: false
          prerelease: false

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs
          destination_dir: .
          keep_files: true
